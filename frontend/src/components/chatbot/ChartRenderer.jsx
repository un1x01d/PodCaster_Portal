// Placeholder for now as the Chatbot delegates chart rendering to the main Dashboard via onUpdateChart.
// If future requirements need inline charts in the chat bubble, they goes here.
import React from 'react';

export default function ChartRenderer() {
    return null; 
}