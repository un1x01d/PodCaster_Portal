#!/usr/bin/env bash

# Launch command used by scripts/codex-start.sh
CODX_CMD="codex"

# Configure memory directory used by codex-start.sh.
# All `.md` files in this directory will be loaded automatically.
CODER_MEMORY_DIR="${CODER_MEMORY_DIR:-/home/zed/.codex/memories}"
